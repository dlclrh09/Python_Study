# None = 값이 없다 True, False 값처럼 N 대문자로 시작해야함
# None 은 True, False: 'False' 값 출력
# None도 하나의 객체이기 때문에 변수 설정 가능

# None 사용 다음과 같다 > 함수에 결과를 return이 아닌 print()로 출력해도 None 나옴
# 1. 값이 없을 때
# 2. 값이 존재 하지 않거나 알 수 없을 경우
# 3. 값이 정의되지 않는 경우
# 4. 값을 초기화할 경우

# 함수에 결과를 return이 아닌 print()로 출력해도 None 나옴
# 위의 예시

# def sum(a, b):
#     return a + b  # 왜 함수를 return 해서 출력해야 할까? 앞에서는 따로 안 했는데;


# print(sum(9, 4))

def spam (spam):
    print('Hello!')
None == spam
